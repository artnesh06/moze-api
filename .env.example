PORT=3000
HOST=0.0.0.0

# Comma-separated browser origins allowed to call the API
CORS_ORIGIN=https://www.mozestreet.art,https://mozestreet.art,http://localhost:8765,http://127.0.0.1:8765

# Robinhood Chain
RPC_URL=https://rpc.mainnet.chain.robinhood.com
CHAIN_ID=4663
MOZE_CA=0x0e579bcec21ae9dc5400db46cab67d5a8d0a58cc

# Soft stake rate (same as frontend)
MOZE_RATE_PER_DAY=10

# SQLite path (mount a volume at /data in Coolify)
DATABASE_PATH=./data/moze.db

# Holder scan cache TTL (ms) — default 10 min
HOLDERS_CACHE_TTL_MS=600000

# Max supply to scan for ownerOf
MAX_SUPPLY=1000

# Signature validity window (ms)
SIG_MAX_AGE_MS=600000

# Optional: OpenSea API key for floor / volume / sales on GET /v1/stats
# Without it, minted + holders still work (RPC + on-chain holder scan)
# OPENSEA_API_KEY=
# OPENSEA_SLUG=mozestreetart
# STATS_CACHE_TTL_MS=60000

# Soft-$MOZE raffle (synced on API start)
# Local/dev default: 0.1 · Production (NODE_ENV=production) default: 11
# RAFFLE_TICKET_COST=11
# RAFFLE_MAX_TICKETS_PER_WALLET=20
# RAFFLE_ENDS_IN_DAYS=14
# RAFFLE_PRIZE_LABEL=Moze #30 (founder)


